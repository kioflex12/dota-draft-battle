# Беклог

Живой список работ. Порядок внутри эпика — по приоритету: **P1** делаем сейчас, **P2** следом,
**P3** когда дойдут руки. Эпик A взят в работу первым.

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` сделано.

---

## A. UI и флоу драфта — **в работе**

Пункты A1–A10 — из списка заказчика, A11+ — найдены при проходе по коду.

### Что держим в голове во всём эпике

- **Режимов три, и правка обязана работать во всех:** игра с другом (`pvp`), с ботом (`bot`)
  и вдвоём за одним экраном (`local`). Горячее место — `local`: там `myTurn`
  (`public/js/app.js:380`) отдаёт ход **любой** стороны, один человек играет за обе по очереди.
  Логика вида «мой герой / моя сторона», закэшированная один раз, здесь рассыплется.
- **Автоматической проверки для экрана драфта нет** (пункт C1 её не даёт — он про движок, не
  про DOM). Гейт эпика A — руками в браузере, во всех трёх режимах.

### P1

- [x] **A1. Линии и позиции после драфта расставляются сами — нужна ручная расстановка.**
  Номер позиции на карточке героя в разборе стал выбором: ставишь герою позицию, тот, кто её
  занимал, забирает освободившуюся, и весь разбор пересчитывается под заявленную раскладку —
  линии, штрафы за роль, состав, итоговая оценка. Кнопка «вернуть авто» возвращает догадку
  движка. `analyze()` принимает `{ posRadiant, posDire }` и проверяет, что это перестановка.
  Вкладка «Альтернативы» тоже следует заявленной раскладке: замена подбирается на ту позицию,
  которую герой занимает по ней, а не по догадке движка.

- [x] **A2. Полоска силы во время драфта — убрать, оставить только в результате.**
  `#live-meter` пересчитывается на каждый пик (`public/js/app.js`, `renderDraft`) и висит между
  рядами банов. Во время битвы это подсказка капитану. Убрать с экрана драфта; в разборе
  оценка и так есть.

- [x] **A3. Баны — крупнее и под пиками.**
  Сейчас ряд банов стоит **над** сеткой, в одной строке с полоской силы (`index.html`,
  `.bans-row`), и сами плашки мелкие. Перенести под пики, увеличить.

- [x] **A4. На экране драфта не видно, у кого первый пик.**
  Проверено: очерёдность в коде **верна** и совпадает с патчем 7.34+ — первый пик банит
  3-2-2, второй 4-1-2 (`shared/draft.js`, `SEQUENCE`; сверено с esports.gg и GosuGamers).
  Но на экране драфта об этом нет ни слова: о первом пике сообщается один раз строкой в чате
  (`shared/room.js:143`), дальше догадывайся сам — отсюда и ощущение, что «первый пик забанил
  4 раза в первой фазе». Поставить у команды явную отметку «первый пик» и показывать,
  сколько банов у кого осталось в текущей фазе.

- [x] **A5. Карточки героев мелкие, сетка неудобная.**
  `.hcell` в `public/css/style.css` — плотная сетка миниатюр на всю ширину. Увеличить карточки,
  пересобрать раскладку так, чтобы герой читался с одного взгляда.

- [x] **A6. Клик по способности перезапускает анимацию героя.**
  `renderHeroPanel` (`public/js/heroPanel.js`) на каждый клик по вкладке или иконке
  перерисовывает **весь** блок через `root.innerHTML`, вместе с `<video>` модели — поэтому
  ролик стартует заново. Перерисовывать только содержимое вкладки, шапку с моделью не трогать.

- [x] **A7. Модели героев: включить по умолчанию и привести в приличный вид.**
  Сейчас анимация выключена по умолчанию (`portraits`, `public/js/app.js:13`), а включённая
  выглядит плохо: у `.webm`-рендера прозрачный фон, из-за чего виден белый контур и пустота
  вместо подложки. Включить по умолчанию и дать модели фон (градиент цвета стороны/атрибута)
  либо перейти на анимированный портрет вместо полной модели.

- [x] **A8. Строка очерёдности пиков и банов — переделать целиком.**
  `#sequence` (`index.html`, стили `.seq-step`) стоит третьей полосой сверху, портреты в ней
  обрезаны по половине героя и растянуты. Вопрос не только в картинках: **место и сама форма
  под вопросом** — нужно решить, как показывать очередь так, чтобы было видно, чей ход,
  что уже сделано и что впереди, не забирая три полосы высоты. Отдельный пункт на проработку,
  делать после A2/A3, когда верх экрана освободится.

- [x] **A9. Полосы прокрутки белые, выпадают из стилистики.**
  Нигде не стилизованы (`public/css/style.css`). Тёмный скроллбар под тему игры —
  `scrollbar-color` плюс `::-webkit-scrollbar`.

- [x] **A10. Двойной клик мгновенно лочит героя — без подтверждения и без отмены.**
  `public/js/app.js:311-314`: `dblclick` по ячейке сразу шлёт `action`. Случайный второй клик
  = потраченный пик или бан, откатить нечем. В Dota выбор подтверждается кнопкой.
  Два рабочих варианта: убрать `dblclick` совсем и оставить кнопку; либо `dblclick` лишь
  взводит подтверждение, а не отправляет ход.

- [x] **A20. Экран результата: сводки общими словами, а не про одну сторону.**
  Кривая по длительности матча была подписана как «шанс победы Сил Света»; теперь читается сразу
  за обе команды: подпись оси называет ведущую сторону («60% Света» выше средней линии, «60% Тьмы»
  ниже), точка показывает оба значения, заголовок — «Чья игра по длительности матча».
  Карточки стадий игры показывают обе стороны (46% : 54%), а не только ведущую. Сноски под
  вкладками переписаны обычными словами: точные размеры выборки остались в одном месте — во
  вкладке «Итог», остальные говорят, на чём основана оценка, без перечисления чисел.

### P2

- [x] **A11. На телефоне драфт практически неиграбелен.**
  Тап по герою выделяет его, а кнопка «Выбрать / Забанить» теперь закреплена внизу экрана
  вместе с портретом выбранного — раньше она жила в боковой панели под всей сеткой героев.
  Шапка на телефоне перестроена в две колонки с мелкими слотами (прежняя занимала весь экран,
  сетка целиком уходила за его пределы), фильтры ролей — одной прокручиваемой строкой.
  Наведение-эффекты убраны на тач-устройствах, где они залипали.
  **Осталось:** подсказки `data-tip` на тач по-прежнему недоступны — информация о герое там
  берётся из панели, но подписи к слотам банов и к полосе очерёдности на телефоне не увидеть.

- [x] **A12. Про свой ход можно не узнать в фоновой вкладке.**
  Заголовок вкладки мигает «● ВАШ ХОД», пока ход ваш и вкладка в фоне; `AudioContext`
  пробуждается из `suspended`, если браузер его усыпил.

- [x] **A13. Наведение отправляется, но нигде не показывается.**
  Клиент шлёт `{t:'hover'}` (`public/js/app.js:390`), сервер рассылает (`shared/room.js:306`),
  `onMessage` (`app.js:78-96`) про это сообщение не знает и молча его выбрасывает.
  Условие рассылки намеренно исключает соперника, а в битве один на один союзников нет —
  значит получатели только **зрители**. Показывать сопернику, на кого смотрит капитан, нельзя.
  Сделана подсветка для зрителей: смотрящий видит, на кого смотрит капитан, соперник — нет.

- [x] **A14. Чат перерисовывается целиком** на каждое обновление комнаты (`renderChat`,
  `public/js/app.js`) — слетает выделение текста. В драфте чат спрятан под панелью героя,
  о новом сообщении ничто не сообщает.

- [x] **A15. Клавиатурой сетку героев не пройти.** Стрелки ходят по сетке (по геометрии, а не по
  порядку в разметке — сетка разбита на четыре блока по атрибутам); при наборе в поиске
  вверх-вниз идут по совпадениям, влево-вправо остаются курсором. Ход по-прежнему
  подтверждается кнопкой или Ctrl+Enter — одиночным Enter специально нельзя.

- [x] **A16. Подсказка залипает** при прокрутке колесом: `bindTooltip` (`public/js/util.js`)
  прячет её только по `mouseout`.

- [x] **A17. Загрузка без прогресса** — `heroes.json` (1.3 МБ) и `stats.json` (0.7 МБ) тянутся
  под неопределённым спиннером (`app.js`, `boot`).

- [x] **A21. Экран жребия был не свёрстан.** Стилей у него не было вовсе — кроме одного правила
  в медиазапросе: монета не рисовалась, варианты выбора шли строкой вместе с пояснениями, и
  человек не понимал, что от него хотят. Собран заново: монета с переворотом на выпавшую
  сторону, заголовок, объяснение правила одним предложением, отдельная строка «что нажать» и
  карточки вариантов столбиком с пояснениями. Стороны покрашены в свои цвета, очередь пика
  оставлена нейтральной — прежняя зелёная подсветка читалась как совет.

### P3

- [x] **A18. Копирование ссылки на телефоне.** Если `navigator.clipboard` недоступен (обычный
  `http://<ip>:3000` — не secure context), поле со ссылкой показывается и выделяется, плюс
  попытка старого `execCommand('copy')`.

- [x] **A19. Доступность.** Появились видимая рамка фокуса, `aria-pressed` у фильтров ролей и
  `aria-live` на смене хода и фазы.

---

## B. Надёжность мультиплеера

Не аудировано построчно — кандидаты по README и беглому чтению `net.js`.

- [x] **B1. Комната живёт, пока открыта вкладка создателя** (P2P на GitHub Pages). Перед
  закрытием вкладки хост получает предупреждение браузера, если в комнате есть кто-то ещё.
  **Главное — найдена настоящая причина жалоб «вкладка открыта, а комната не находится»:**
  комнату держит не вкладка, а регистрация хоста на сигнальном сервере, и она слетает, когда
  браузер усыпляет фоновую вкладку. Теперь хост следит за своей регистрацией (по таймеру, при
  возврате к вкладке и при восстановлении интернета) и восстанавливает её, а пока связи нет —
  честно пишет полосой, что друг сейчас войти не сможет.
  **Осталось:** передача роли хоста другому игроку — не сделана.
- [x] **B2. Гостя при обрыве просто выбрасывает.** Теперь до четырёх попыток вернуться в ту же
  комнату с нарастающей паузой, отдельно — переподключение к сигнальному серверу; сдаёмся только
  после этого. Вход в комнату тоже не сдаётся с первой попытки: хост мог на секунды потерять
  регистрацию. Сообщения об ошибках расшифрованы по типу — вместо «Ошибка сети: network»
  говорится, что именно не сложилось и что с этим делать.
- [x] **B3. `/room/КОД` из README не существует.** Сервер переадресует такой адрес на
  `?room=КОД`, сохраняя остальные параметры. Отдавать по этому пути саму страницу нельзя:
  относительные ссылки на скрипты уехали бы в `/room/js/…` — на этом первая попытка и попалась.
- [x] **B4. Таймер при обрыве** — разобрано по коду: сервер тикает сам (`RoomManager.tick`,
  каждые 250 мс) и по истечении времени делает автоход, независимо от того, на связи ли игрок;
  место за игроком держится по токену, поэтому вернувшийся попадает обратно в свою команду.
  Дыра одна: **без таймеров** ушедший игрок вешает драфт навсегда — партию нечем завершить.

## C. Движок анализа

- [x] **C1. Регрессионной проверки нет вообще.** `npm run check` (`tools/analysis-baseline.mjs`)
  печатает снимок движка на двадцати фиксированных составах — оценка, слагаемые, стадии, кривая,
  линии, позиции — плюс полный драфт Captains Mode ботом и проверки: полоса калибровки,
  симметрия контрпиков, работа ручной раскладки. Снимки до и после правки сравниваются `diff`.
- [x] **C2. Калибровка оценки** — измерено (2026-09-23, патч 7.41f): случайные составы —
  медиана 50.4%, полоса 5–95% это 27.7–72.1%, за 75/25 выходит 5.3% случаев, максимум 83.5%;
  оба капитана сложным ботом — 37.9–58.3%, за 75/25 не выходит ни разу; сложный против лёгкого —
  медиана 66.1%, максимум 79.6%. Заявление «редко выходит за ~75/25» подтверждается: перекос
  дают только заведомо случайные составы. Трогать `CAL` повода нет.
- [ ] **C3. Объяснимость вердикта** для того, кто не читал код.
- [x] **C4. Качество альтернативных пиков.** Замена теперь оценивается и по риску ответного
  пика (C5), и по заявленной вручную раскладке позиций (A1), а не только по матчапу с тем, что
  уже стояло на столе.

- [x] **C5. Альтернативные пики должны смотреть вперёд, а не только назад.**
  К оценке добавлен риск ответного пика: чем героя закроют из того, что на тот момент не было
  забанено и не было взято. Угрозы взвешены по остроте (главную соперник и возьмёт) и по тому,
  насколько правдоподобно, что он вообще возьмёт этого героя. Забаненный контрпик риск снимает —
  поэтому бан теперь делает пик безопаснее и в рекомендациях, и в разборе. Работает и в
  подсказках по ходу драфта (`suggest`), то есть влияет на бота.
- [x] **C6. Оценивать состав целиком, а не сумму линий и героев.**
  Во вкладке «Итог» появился блок «Как это играется»: характер состава (драки / пуш / фарм /
  подвижность), что делать по времени с этим конкретным соперником и во что упирается план.
  Совет по времени считается по раскладу с соперником, а не по своей кривой, иначе строки
  противоречили друг другу.
  **Оговорка:** это разбор словами, отдельного слагаемого в оценке не добавлено — счёт остаётся
  прежним. Слагаемое «состав команды» как было, так и весит около половины процента и часто
  ноль; если нужно, чтобы состав влиял на число, это отдельная работа с калибровкой.
- [x] **C7. Стадии игры должны учитывать, как команда теоретически стоит линии.**
  Перевес на линиях (золото+опыт к 10-й минуте по про-данным) больше не стоит в кривой плоско:
  он затухает от ~1.8 веса на 10-й минуте до ~0.4 на шестидесятой, нормировано так, чтобы в
  среднем по кривой остаться единицей и не сдвинуть общую оценку. Карточки стадий считаются по
  той же кривой, что нарисована рядом, иначе они с ней спорили.

## D. Бот

- [x] **D1. Разница между уровнями сложности** ощущалась слабо — и это подтвердилось замером:
  «сложный» против «среднего» давал 52.9% (почти ничья), потому что уровни отличались только
  случайностью выбора из одного и того же списка. Теперь отличается сам взгляд на драфт: слабый
  бот видит только силу героя в патче, сильный — связки, контрпики и риск ответного пика. После правки:
  сложный против среднего 57.3%, средний против лёгкого 71.7%, равные уровни около 50%.
- [x] **D2. Осмысленность банов** — бан тоже зависит от уровня: слабый банит по популярности
  героя, сильный — по тому, насколько герой опасен именно в этом раскладе (насколько он усилил бы
  состав соперника против нашего).
- [x] **D3. Ответ на вражеский драфт** — учитывает: оценка кандидата считается против уже
  собранного состава соперника, а с пунктом C5 добавился и взгляд вперёд — чем соперник сможет
  ответить из того, что осталось в пуле. У слабого бота этот учёт ослаблен намеренно.

## E. Данные и инфраструктура

- [ ] **E1. Свежесть статистики.** `data/stats.json` собран 2026-09-23 по патчу 7.41f —
  актуален. Пересобирать (`npm run build-data`, ~15 минут) при смене патча; константы
  `PATCH`, `PATCH_TS`, `PRO_SINCE` в `tools/build-data.mjs:9-11`.
- [ ] **E2. Расхождения README и кода** — поправить одним заходом (см. B3).
